<%= button_to "Sign Out", destroy_user_session_path, method: :delete %>
<h1>List all users</h1>
<div>
  <% @users.each do |user| %>
    <div><%= render 'user', user: user %></div>
    <p><%= link_to 'More info', user_path(user.id) %></p>
  <% end %>
</div>
